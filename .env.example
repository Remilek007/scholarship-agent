# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/scholarship_agent

# API
PORT=4000

# Discovery: comma-separated official/direct pages to crawl
DISCOVERY_DIRECT_URLS=

# Discovery: comma-separated RSS/Atom feeds
DISCOVERY_RSS_FEEDS=

# Optional generic search adapter
DISCOVERY_SEARCH_ENDPOINT=
DISCOVERY_SEARCH_API_KEY=

# Optional provider-specific search keys
BRAVE_SEARCH_API_KEY=
TAVILY_API_KEY=

# Optional self-hosted SearXNG metasearch endpoint
SEARXNG_URL=
# Optional comma-separated SearXNG engines. Leave empty to use the configured default set.
SEARXNG_ENGINES=google,bing,brave,duckduckgo

# Bounded discovery controls
DISCOVERY_MAX_PAGES=250
DISCOVERY_MAX_DEPTH=3
DISCOVERY_CONCURRENCY=6
DISCOVERY_REQUEST_TIMEOUT_MS=20000
DISCOVERY_PLAYWRIGHT_ENABLED=false

# Optional AI provider
OPENAI_API_KEY=

# Optional built-in scheduler. Set both values to enable periodic discovery.
# JSON must contain nationality, degreeLevel, targetFields and minimumFunding.
DISCOVERY_INTERVAL_MINUTES=0
DISCOVERY_PROFILE_JSON=
